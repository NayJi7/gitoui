# build output
dist/
.astro/

# dependencies
node_modules/

# Claude skills cache (per-machine, auto-regenerated)
skills-lock.json
.agents/

# Synced from repo root by the `sync-public` npm script — never edit
# directly here. Source of truth lives at ../install.sh and
# ../assets/brand/. Re-runs on every `npm run dev` / `npm run build`.
public/install.sh
public/brand/

# environment files
.env
.env.production

# old mdBook output (kept around once during migration to compare,
# safe to remove from history later)
book/
mdbook
